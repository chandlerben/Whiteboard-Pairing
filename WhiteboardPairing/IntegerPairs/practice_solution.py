# Create a hash that we can add elements to as we iterate through it.


def integer_pairs(arr, k):
    hash = {}

# Create a if else that determines if even one pair is found. (start our false, change to true when a pair is found)
# Iterate over the array.  As we go look for two things.
    # If the k - i is in the hash,
    #   print(f"{k, i}")
    # change found to True
    # Else
    # Add the i to the hash table


# If no pairs found(found = False) print "No Pairs Found"
